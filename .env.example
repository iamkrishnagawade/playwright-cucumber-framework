# Browser Configuration
# Set to 'false' to run tests in headed mode (visible browser)
HEADLESS=true

# Slow down execution by specified milliseconds (useful for debugging)
SLOW_MO=0

# Application URL
# Override the default base URL for the application under test
BASE_URL=https://www.saucedemo.com

# Cucumber Tags
# Run specific tests by tags (e.g., '@smoke', '@regression', 'not @skip')
# TAGS=@smoke
